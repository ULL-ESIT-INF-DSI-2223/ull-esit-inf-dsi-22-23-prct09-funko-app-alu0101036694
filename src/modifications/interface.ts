/**
 * @class SortingInterface
 * @description Interfaz que define el método sort
 * @version 1.0.0
 * @public
 * @method sort
 * @param {Array} arr - Array de números a ordenar
 * @returns {Array} - Array de números ordenados
 */
export interface SortingInterface {
  sort(arr: number[]);
}
